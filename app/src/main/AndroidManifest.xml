<?xml version="1.0" encoding="UTF-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.MainApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".StartActivity"
            android:label="@string/title_activity_compose_start"
            android:theme="@style/AppTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


            <!-- deep link with URI: atomicrobot://carbon-android -->
            <!-- Test from adb shell using: -->
            <!-- adb shell am start -W -a android.intent.action.VIEW -d "atomicrobot://carbon-android" -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="atomicrobot"
                    android:host="carbon-android" />
            </intent-filter>

            <!-- deep link with URI: http://www.atomicrobot.com/carbon-android -->
            <!-- Test from adb shell using: -->
            <!-- adb shell am start -W -a android.intent.action.VIEW -d "http://www.atomicrobot.com/carbon-android" -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http"/>
                <data android:scheme="https"/>
                <data android:host="*.atomicrobot.com"/>
                <data android:pathPrefix="/carbon-android"/>
            </intent-filter>

        </activity>
        <!-- GENERATOR - MORE ACTIVITIES -->

        <activity-alias
            android:name=".Launcher"
            android:targetActivity=".StartActivity" />

    </application>

</manifest>
