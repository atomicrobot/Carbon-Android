package {{package}}.ui.{{screenLowerCase}}

import android.content.Context
import {{package}}.ui.BasePresenter
import {{package}}.ui.{{screenLowerCase}}.{{screenUpperCamel}}Presenter.{{screenUpperCamel}}ViewContract
import org.parceler.Parcel

class {{screenUpperCamel}}Presenter(private val context: Context)
    : BasePresenter<{{screenUpperCamel}}ViewContract, {{screenUpperCamel}}Presenter.State>(STATE_KEY, State()) {

    interface {{screenUpperCamel}}ViewContract

    @Parcel(Parcel.Serialization.BEAN)
    data class State(
        var initialized: Boolean = false)

    override fun onResume() {
        super.onResume()
        if (!state.initialized) {
            state.initialized = true
        }
    }
    companion object {
        private const val STATE_KEY = "{{screenUpperCamel}}PresenterState"  // NON-NLS
    }
}
